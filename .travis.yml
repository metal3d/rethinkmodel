---
langage: python
python:
  - 3.9
addons:
  rethinkdb: 2.4

env:
  RM_HOST: localhost

install:
  - pip install pipenv
  - pipenv install --dev

script:
  - make test

after_success:
  - bash <(curl -s https://codecov.io/bash)
